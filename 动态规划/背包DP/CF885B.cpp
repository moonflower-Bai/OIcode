#include <bits/stdc++.h>
using namespace std;
const int N = 1e6 + 5;
int n, p, q, r;
int a[N];
int f[N][5];
int main()
{
	cin >> n >> p >> q >> r;
	for (int i = 1; i <= n; i++) {
		cin >> a[i];
	}
	return 0;
}